# Check a logical argument is in a suitable format

Check if a given argument is of a logical. If not it will give an
informative error message.

## Usage

``` r
check_logical(arg_lgl)
```

## Arguments

- arg_lgl:

  The argument to check
